{
  "name": "Isochrone",
  "version": "1.0.0",
  "scripts": {
    "build": "cp -r src/* docs/",
    "deploy": "npm run build && gh-pages -d docs"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
